package template;

public class Recursion {
    /*
    找重复子问题
    数学归纳法

    public void recursion(int level,int param) {
        // 终止条件
        if (level>MAX_LEVEL) {
            return;
        }

        // 处理当前层的逻辑
        process(level, param);

        //到下一层
        recursion(level + 1, new param);

        // 清理当前层
    }

     */
}
